model Address {
  id        Int      @id @default(autoincrement())
  street    String
  number    String?
  complement String?
  neighborhood String?
  city      String
  state     String
  zipCode   String

  offers Offer[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
